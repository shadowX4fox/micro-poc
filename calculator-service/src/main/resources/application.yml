micronaut:
    application:
        name: calculator-service
    server:
        port: 9000
kafka:
    bootstrap:
        servers: localhost:9092
consul:
    client:
        registration:
            enabled: true
        defaultZone: "${CONSUL_HOST:localhost}:${CONSUL_PORT:8500}"
